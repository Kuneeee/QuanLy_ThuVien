# Application Configuration
spring.application.name=quan-ly-ban-hang

# Server Configuration
server.port=8080
server.servlet.context-path=
server.use-forward-headers=true
server.forward-headers-strategy=framework
management.server.port=8080

# Neon Database (PostgreSQL) Configuration
spring.datasource.url=jdbc:postgresql://ep-silent-boat-a14wejrc-pooler.ap-southeast-1.aws.neon.tech:5432/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_LuahzTi3C2Ac
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection Pool Settings (Optimized for Neon)
spring.datasource.hikari.connection-timeout=10000
spring.datasource.hikari.validation-timeout=3000
spring.datasource.hikari.max-lifetime=300000
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1

# JPA/Hibernate Configuration for PostgreSQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Database Initialization (disabled - use existing database data only)
spring.sql.init.mode=never
spring.sql.init.continue-on-error=false
spring.jpa.defer-datasource-initialization=true

# Logging Configuration
logging.level.com.example=INFO
logging.level.controller=DEBUG
logging.level.org.hibernate.SQL=DEBUG